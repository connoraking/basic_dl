# basic_dl
mle practice with different pipelines for different goals

Uses a google colab notebook to train and test the models because I have an AMD GPU not NVIDIA (therefore no CUDA). Google colab lets me use NVIDIA GPU's. 
