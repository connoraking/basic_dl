# basic_dl
mle practice with different pipelines for different goals

Uses a google colab notebook to train and test the models because I have an AMD GPU not NVIDIA (therefore no CUDA). Google colab lets me use NVIDIA GPU's. When I confirm that the scripts run fine, I will clone the repo on my brother's PC (5070 TI) and it'll create an experiments folder with outputs
